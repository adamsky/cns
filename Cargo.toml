[package]
name = "cns"
version = "0.1.0"
authors = ["adamsky <adamsky@enum.space>"]
edition = "2018"
description = "Crate name search."

[dependencies]
tui = { version = "0.10", default-features = false, features = ["crossterm"] }
crossterm = "0.17"
crates_io_api = "0.6.1"
webbrowser = "0.5.5"
curl = "0.4.33"
strip_markdown = "0.2.0"
